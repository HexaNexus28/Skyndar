<UserControl x:Class="WPF.Views.Historique"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
            
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="RoundedButton">
            <Setter Property="Background" Value="#5271ff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#7ba6fe" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <DataGrid ItemsSource="{Binding Historique}" SelectedItem="{Binding SelectedRendezVous, Mode=TwoWay}" AutoGenerateColumns="False" Margin="25,10,25,40" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nom" Binding="{Binding User.Username}" />
                    <DataGridTextColumn Header="Email" Binding="{Binding User.Email}" />
                    <DataGridTextColumn Header="Prestation" Binding="{Binding Prestation.Titre}" />
                    <DataGridTextColumn Header="Date" Binding="{Binding CalendarDay.Date}" />
                    <DataGridTextColumn Header="Tarif (€)" Binding="{Binding Prestation.Tarif}" />
                    <DataGridTextColumn Header="Heure de Debut" Binding="{Binding Creneau.HeureDebut}" />
                    <DataGridTextColumn Header="Heure de Fin " Binding="{Binding Creneau.HeureFin}" />
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Supprimer un rendez-vous" Width="158" Height="25" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="617,0,0,10" Click="Supprimer_Click" Cursor="Hand" Style="{StaticResource RoundedButton}"/>
        </Grid>
    </Grid>
</UserControl>
